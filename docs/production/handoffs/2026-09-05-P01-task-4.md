# Session handoff

## Work identity

- Plan and task IDs: **P01 Task 4 — Restrict outbound URL validation and provider destinations**, complete. **Task 5 not started.** Native signed Keychain/startup remains a P06 gate; no production-readiness claim.
- Branch/worktree and commit: `production/p01-task1`, `/Users/m1labs/Dev/apps/juno/.worktrees/p01-task1`. Started clean at `35aa233`, following Task 3 implementation `c5801df`. Implementation/tests/shared contract/caller map: **`5649a71`**. This handoff and roadmap are the following documentation commit. Worktree and branch retained; no merge or push.
- Starting workspace changes not owned by this task: none in the isolated worktree. Original checkout still has unrelated `notron.egg-info/PKG-INFO`, `notron.egg-info/SOURCES.txt`, `.claude/`, `docs/plans/2026-09-03-pin-her-notes.md`, and `notrontask.md`. No original-checkout files were edited, staged or merged.
- Implemented behavior: pure `public_https_url(url, addresses)` and canonical provider endpoint validation; one guarded transport for inference, embeddings, model listing and search. Exact HTTPS service host/method/path/443 allowlists; all unsafe or mixed DNS sets denied, direct numeric connection with peer check and original-host verified TLS. No generic fetch, proxy routing or redirects. Explicit development override uses a separate allowlisted injected credential, never production/environment/constructor fallback. Transport rebuilds fixed JSON/authentication headers, so SDK ambient headers/cookies cannot leak credentials. Late credential/storage/policy failures remain fail-closed through SDK handling. Writer/planner citations use exact prepared source URLs locally, without DNS/HEAD/HTTP; unsupported citations are marked unverifiable. All prior policy/provenance/storage protections and P06 startup gate preserved. Test infrastructure now denies unmocked DNS globally.
- Explicitly not implemented: Task 5 security inventory/dependency vulnerability audit, P02 durable execution/concurrency, hosted managed service, signed/native Keychain integration, real-data processing/migration, real provider/TLS verification, deployment, merge or push.
- Stack: Python 3.11+ project; **Python 3.14.2** tests; existing OpenAI SDK **3.7.0** and **httpx2 2.12.0** now explicitly pinned, Python stdlib HTTP/socket/TLS; Swift Security helper name allowlist only. No packages installed or upgraded; lock refreshed offline. Nebius models remain unchanged: `nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B`, `nvidia/nemotron-3-super-120b-a12b`, `nvidia/Nemotron-3-Ultra-550b-a55b`; embedding `Qwen/Qwen3-Embedding-8B`. Tavily remains search. No actual AI model calls.

## Evidence

All pytest commands ran in the isolated worktree using
`/Users/m1labs/Dev/apps/juno/.venv/bin/python -m pytest` (shown as `pytest` below).
No worktree-local venv was created. Fixtures used synthetic temporary files and
mocked Apple, Keychain, inference, embedding and search integrations. The new
transport tests keep the actual SDK, HTTP client and HTTP parser above fake DNS,
sockets and TLS; they inspect outgoing synthetic bytes and connection decisions.

**Test isolation incident:** on the first post-implementation full run, eight old
search/privacy tests still patched urllib rather than the new adapter. The existing
socket guard blocked provider HTTP, but the fixed public Tavily domain was resolved
through DNS before that block. No provider HTTP/citation request, real credential
or user content was sent. The obsolete fixture was replaced and a global
`getaddrinfo` deny guard added. All final runs used mocked DNS. No real Notes,
Reminders, Calendar, credentials, Keychain entries or private caches were accessed.

| Command or manual case | Actual result / exit code | Artifact |
|---|---|---|
| Initial `git status --short`, branch and recent log | Clean expected branch, HEAD `35aa233`; exit 0 | Work identity |
| `pytest tests -o addopts='' -q` baseline | **489 passed in 3.57s**, exit 0 | Task 3 baseline |
| Initial Task 4 network/citation/research regressions before implementation | **105 failed, 20 passed in 0.92s**, pytest exit 1 | `tests/test_network.py`, `tests/test_citations.py` |
| Additional citation regressions before fixes | **3 failed, 138 passed**, exit 1: exact-prefix corruption, adjacent Markdown URLs, planner grounding | `tests/test_citations.py` |
| SDK ambient-header regression before fix | **1 failed**, exit 1: production-like ambient authorization forwarded to development host | `tests/test_network.py` |
| Inference timeout regression before fix | **1 failed**, exit 1: adapter had shortened SDK reasoning timeout | Same |
| Late secure-boundary regressions before fix | **3 failed**, exit 1: SDK wrapped credential/storage/policy failures as availability errors | Same |
| First full run after transport replacement | **8 failed, 585 passed**, exit 1: obsolete urllib mocks; blocked HTTP, DNS incident documented above | Updated `tests/conftest.py` |
| `pytest tests/test_network.py tests/test_citations.py tests/test_research.py -o addopts='' -q` final | **156 passed in 1.20s**, exit 0 | Required Task 4 tests |
| `pytest tests/test_outbound.py tests/test_privacy.py tests/test_policy.py tests/test_graph.py tests/test_credentials.py tests/test_securestore.py tests/test_cache_migration.py tests/test_library.py tests/test_filer.py tests/test_reflect.py tests/test_care.py tests/test_nodes.py tests/test_watch.py tests/test_undo.py tests/test_executor.py tests/test_persistence.py -o addopts='' -q` final | **345 passed in 3.36s**, exit 0 | Adjacent outbound/privacy/policy/graph/credential/storage regressions |
| `pytest tests -o addopts='' -q` final | **624 passed in 4.59s**, exit 0; 135 above baseline | Full Python suite |
| `swiftc -typecheck -D NOTRON_KEYCHAIN_HELPER -parse-as-library mac/Sources/Notron/KeychainStore.swift` | Exit 0, no warnings | Source type-check only; helper never launched |
| `uv lock --check --offline` | Exit 0, 27 packages resolved | Existing dependency versions retained |
| Complete caller/transport `rg` audits | Exit 0; all four actual HTTP operations accounted for, no generic/citation fetch | [Updated outbound map](../evidence/P01-outbound-map.md) |
| Independent read-only review and focused re-reviews | Ambient headers and late secure-recheck findings fixed after failing regressions; final review found no blocking issues and independently reran **156 passing** targeted tests | Network/citation/research suite |
| `git diff --check`, `git diff --cached --check` | Exit 0 | Implementation commit |

Additional real-boundary tests cover every service's blocked/mixed/empty DNS,
public IPv4/IPv6, DNS rebinding, TLS verification/name, mismatched peer, all usual
redirect codes, relative/same-host/downgrade/private redirects, proxy environment,
SDK endpoint mutation, unauthorized routes/Host headers, development-mode/key
revocation, constructor/environment-key fallback, and zero network for invented
citation URLs. Prepared citation tests cover redaction, ignored notes, model-origin
self-grounding, exact URL matching, wrappers and more than five unsupported links.

## State and recovery

- Schema/configuration changes and migrations exercised: no policy, cache or storage schema change and no data migration. `DEV_NEBIUS_KEY = development-nebius-api-key` extends the existing Python/Swift credential allowlist. A nondefault public HTTPS Nebius-compatible host under `NEBIUS_BASE_URL` requires `NOTRON_DEVELOPMENT=1` and this injected account. Default service URLs/model settings are unchanged. SDK automatic retries are disabled; Brain's existing reasoning retry still rechecks/prepares. Nebius read/write timeout remains 600 seconds with a five-second connect budget; Tavily remains 20 seconds.
- What is running and how to stop it: no listener, app, helper, launchd job or service was started. Test/compiler processes exited. Existing processes untouched.
- External services touched: incidental public DNS resolution of the fixed Tavily hostname in the initial obsolete-fixture run; no provider/citation HTTP or live Apple/Keychain calls. No external documentation/downloads/package installation; SDK behavior inspected from locally installed source and lock update performed offline.
- Backup / rollback procedure exercised: no migration or user-data rollback needed. Task 3 recovery/encryption regressions still pass with synthetic data. Code can be reverted via the task implementation and following documentation commits, but reverting network restrictions must not authorize real-data processing. The P06 startup gate remains mandatory.
- Remaining risks or failed cases: no known failing Task 4 regression or blocking review finding. Grounded URLs establish provenance, not factual correctness or reachability; prepared web/user content can itself be wrong. Redaction remains imperfect. Provider service/TLS availability, provider retention, native signed Keychain/permission/installation behavior and compromised-local-account protection are not established by these tests. Redirects and nonapproved endpoints deliberately fail closed. The adapter supports only the current buffered operations; future streaming/new routes/managed endpoints require explicit contract and boundary tests. Task 5 audit/release-facing assertions remain outstanding. P02 transactions and native P06/P07 gates remain open.

## Next session

- Exact next task and first command: **P01 Task 5 — Security inventory and release-facing assertions.** First run `git status --short` in `/Users/m1labs/Dev/apps/juno/.worktrees/p01-task1`, then inspect branch/recent commits and read this handoff, `CLAUDE.md`, the production README/design, P01 plan and current evidence. Use the same root venv. Baseline now **624 passing Python tests**. Do not merge or push this retained branch without a new instruction.
- Inputs that block only that task: no blocker for synthetic security tests/inventory. The owner must choose the private vulnerability reporting route before public release; do not invent/contact recipients or treat the audit as release approval. Native Keychain startup remains a P06 gate and must not be bypassed for testing.
- Decisions made, with links to updated contracts: remove generic citation fetching entirely; one fixed-route provider adapter with numeric DNS pinning and original-host TLS; reject all redirects; rebuild only permitted headers; use the existing credential contract with a dedicated development account; exact prepared URL grounding and sanitized fail-closed exceptions. See [Task 4 shared contract](../design.md#p01-task-4-implemented-network-contract-2026-09-05) and [HTTP caller map](../evidence/P01-outbound-map.md).
- Roadmap checkboxes changed and evidence supporting each: all four Task 4 items checked based on failing regressions, **156 targeted / 345 adjacent / 624 full-suite passes**, exhaustive HTTP audit and final independent review. P01 status is **Tasks 1–4 complete; Task 5 next**. Task 5, M1, P01-wide exit gate and P06/P07 native release gates remain unchecked.
