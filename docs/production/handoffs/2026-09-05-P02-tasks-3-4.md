# P02 Tasks 3–4 session handoff

## Work identity

- Scope: `docs/production/plans/02-reliable-execution.md`, Tasks 3 and 4.
- Branch: `production/p01-task1`.
- Worktree: `/Users/m1labs/Dev/apps/juno/.worktrees/p01-task1`.
- Base: `8426142` (reviewed P02 Tasks 1–2). Implementation: `fb8db5e`, Task 3 correction `4da67f4`, Task 4 `4431ee0`, Task 4 correction `088e7f9`, final receipt-completion correction `bc44082`.
- Root checkout stays on `fix/folder-index-drift`. Unrelated egg-info changes and untracked `.claude/`, `notrontask.md`, and pin-her-notes document are preserved.
- Stack: Python 3.11+, SQLite, AES-GCM encrypted local storage, AppleScript/JXA/EventKit. No Swift UI, dependency, AI provider or model configuration change. Recovery and undo use no model; receipt repair reuses saved successful inference.

## Implemented behavior

Task 3 persists action identity before save and external outcome before its receipt. Exact operation references reconcile reminders/events; inconclusive results require review. Graph checkpoints repair receipts without repeating successful inference. Filing persists exact copy/create operations before effects, keeping IDs, source provenance, target, date and rendered content stable across restarts. A missing source checkmark cannot cause a verified destination copy to repeat. Audit is independent, bounded metadata with asynchronous delivery and backfill.

Task 4 stages encrypted preimages before writes and activates snapshots only after verified success. Failed or uncertain restores retain recovery data. Restore requires the exact snapshot and stored post-write revision and consumes only after verification. An intervening edit—including a newly typed undo command—uses an explicitly confirmed recovery copy. Copies preserve later source words, retain the snapshot, grant no automatic filing-home permission and render historical commands as inactive text so the watcher cannot repeat them.

Detailed implementation, migration and regression evidence:

- [Task 3](2026-09-05-P02-task-3.md)
- [Task 4](2026-09-05-P02-task-4.md)

## Verification and review

All tests use synthetic Apple stores and temporary encrypted state through autouse test guards. No live Notes/Reminders/Calendar, provider calls, real Keychain, listener restart, deployment, merge or push was performed.

| Verification | Result |
|---|---|
| Baseline full Python suite | 798 passed; exit 0 |
| Task 3 full suite after corrections | 847 passed; exit 0 |
| Task 3 independent re-review suite | 207 passed; spec and quality approved |
| Root independent Task 3 filing recovery suite | 20 passed; exit 0 |
| Task 4 full suite after corrections | 882 passed; exit 0 |
| Root independent undo/executor/write-race/node/graph/rewrite suite | 171 passed; exit 0 |
| Task 4 independent re-review suite | 201 passed; spec and quality approved |
| Root full Python suite before final correction | 882 passed in 18.87s; exit 0 |
| Final independent combined review | 248 passed; spec and quality approved through `bc44082` |
| Root final full Python suite | 889 passed in 44.03s; exit 0 |

The exact full command from this worktree is:

```sh
/Users/m1labs/Dev/apps/juno/.venv/bin/python -m pytest tests -o addopts='' -q
```

Initial Task 3 review reproduced duplicate filing caused by dictionary-order-dependent operation IDs, next-day payload changes, and denied same-title candidate reads. All were corrected and independently re-reviewed. Initial Task 4 review reproduced historical command reactivation and successful copy flows invalidating their own completion records. Corrections use inactive history and remove unsolicited home registration; no privacy-purge exception was added. Final combined review then reproduced a source filing checkmark masking a failed queued Ask reply. Commit `bc44082` corrected completion semantics and added seven graph/watcher restart and cooldown cases.

Final whole-batch review approved the requested local implementation with no remaining important findings. See [final review evidence](../evidence/P02-tasks-3-4-review.md). The roadmap marks only Tasks 3–4 complete; Tasks 5–6 and native release gates remain open. Final working-tree whitespace checks passed.

## State, rollback and limits

- SQLite remains schema 3. Filing/approval retry plans now persist exact version-2 Write payloads; legacy incomplete plans require review instead of being reconstructed from current time/order.
- Encrypted undo remains note-keyed, now with `current` and optional `pending` snapshots. Legacy encrypted strings remain recovery-only without invented revision evidence. Ignore/deletion purges both slots. Older code cannot read the new snapshot shape.
- No real-data migration or rollback was exercised. Use compatible code or restore a matched offline backup of ledger, encrypted payloads and configuration with the worker stopped. Never delete history or downgrade evidence to force a retry.
- Lost creation IDs cannot be recovered by scanning unselected same-title bodies. The operation stays under review; existing source lines and the single created copy remain. A delayed sync or ambiguous reference is likewise not permission to save again.
- Filing-approved home registration retains the earlier conservative P01 purge behavior after both receipts finish; it may leave a content-free parent review tombstone. Recovery copies avoid that registration entirely.
- An unresolved staged preimage intentionally blocks further ordinary writes until reconciliation; a recovery copy does not assert the uncertain write succeeded.
- Audit retention is seven days / 200 queue records, five attempted deliveries per pass. Old/unverifiable audit delivery can be dropped; primary effect identities remain.
- Apple Notes exposes no atomic compare-and-swap. Remote iCloud/editor changes in the final read/write gap remain a risk. Exact HTML matching can conservatively refuse native normalization. Native EventKit reference/sync behavior, rich-content fidelity, signed secure startup, and multi-device recovery remain later native validation gates.
- Recovery copies preserve readable text, not embedded Apple objects. P06 secure-startup and P07 security/release gates remain open. These commits are not a release-readiness claim.

## Next session

Continue with **P02 Task 5 — Time, target ambiguity and fresh context**. Task 6 worker lifecycle and maintenance admission also remain open. Read the roadmap, shared design, P02 plan, CLAUDE.md and this handoff, then inspect the retained worktree:

```sh
git -C /Users/m1labs/Dev/apps/juno/.worktrees/p01-task1 status --short --branch
```
