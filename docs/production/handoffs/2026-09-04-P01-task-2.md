# Session handoff

## Work identity

- Plan and task IDs: **P01 Task 2 — All model/search inputs carry provenance**, complete. Task 3 not started.
- Branch/worktree and commit: `production/p01-task1`, `/Users/m1labs/Dev/apps/juno/.worktrees/p01-task1`. Implementation, tests, shared contract and caller map: **`e37bd3d`**. This handoff and roadmap updates are in the following documentation commit. Starting HEAD was `c3c5941`, following Task 1 implementation `2b3914d`. Worktree retained; no merge, push or deployment.
- Starting workspace changes not owned by this task: none in the isolated worktree. Original checkout retained modified `notron.egg-info/PKG-INFO`, `notron.egg-info/SOURCES.txt`; untracked `.claude/`, `docs/plans/2026-09-03-pin-her-notes.md`, `notrontask.md`. Final original-checkout status matches the initial status. No newer handoffs existed at session start.
- Implemented behavior: typed outbound `Passage`, origin/purpose validation using Task 1 policy; all nine inference, two embedding and one search callers converted; boundary redaction including direct requests and approved ordinary notes; IDs retained across watcher/graph, keyword/semantic retrieval, filing and reflection; registered standing-context roles; full-note filtering before chunking/index persistence; pre-provenance index exclusion and local quarantine; current-policy checks before transport and persistence. Static instructions remain separate. Filer groups survive actual transport, sanitized labels resolve locally, ambiguous labels cannot choose another home. Model routing cannot start filing/undo/organizing, and scheduler refuses unsupported operation pairs and cannot obtain operations from retrieved context. Task 1 permissions and reply capabilities preserved.
- Explicitly not implemented: Task 3 Keychain/encryption, retention, cache deletion/purge and encrypted migration; Task 4 citation/URL/provider-destination restrictions; Task 5 release security inventory; P02 durable operation/recovery contracts; native signing/permission work. No provider/model changes.
- Stack: Python 3.11+ project; Python **3.14.2** and pytest used for verification; existing OpenAI-compatible SDK against **Nebius**. Inference defaults: `nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B` for routing/scheduling and `nvidia/nemotron-3-super-120b-a12b` for writing/filing/planning; configured deep tier `nvidia/Nemotron-3-Ultra-550b-a55b`; embeddings `Qwen/Qwen3-Embedding-8B`. No Swift changes.

## Evidence

All tests used synthetic data and mocked Apple, inference, embedding and search integrations. Real Brain methods ran against captured fake transport adapters. Shared fixtures block live network, disable environment-file loading, remove provider keys for tests, and redirect policy/index/filer/reflection/usage/mood/undo/rewrite/mention state to temporary directories. No real Notes, Reminders, Calendar, credentials or private caches were read or modified. No provider calls or native/manual device tests were performed.

Commands below ran in the isolated worktree using `/Users/m1labs/Dev/apps/juno/.venv/bin/python`; there is no worktree-local `.venv` or temporary symlink.

| Command or manual case | Actual result / exit code | Artifact |
|---|---|---|
| Initial `git status --short`, `git branch --show-current`, `git log -6 --oneline`; original checkout status | Clean task worktree, expected branch/commits; original unrelated changes identified; exit 0 | Work identity above |
| `...python -m pytest tests -q` before changes | **409 tests passed**, exit 0 | Task 1 baseline |
| `...python -m pytest tests/test_outbound.py tests/test_privacy.py -o addopts='' -q` before implementation | **17 failed, 15 passed**, exit 1; captured embedding included synthetic password and saved cache retained it; missing API/raw embedding/search bypass exposed | `tests/test_outbound.py` |
| Model-selected filing and invented scheduler operation regressions before fixes | **2 failed, 22 passed**, exit 1 | `tests/test_outbound.py` |
| Transport grouping and redacted-destination regressions before fixes | **2 failed, 38 deselected**, exit 1 | `tests/test_outbound.py` |
| Ambiguous redacted destination / shorter-prefix regressions before fix | **2 failed, 40 deselected**, exit 1 | `tests/test_outbound.py` |
| `...python -m pytest tests/test_outbound.py tests/test_privacy.py -o addopts='' -q` final | **56 passed in 0.14s**, exit 0 | Boundary/privacy tests |
| `...python -m pytest tests/test_outbound.py tests/test_privacy.py tests/test_research.py tests/test_graph.py tests/test_citations.py tests/test_policy.py tests/test_library.py tests/test_filer.py tests/test_reflect.py tests/test_care.py tests/test_nodes.py tests/test_watch.py -o addopts='' -q` final | **312 passed in 2.33s**, exit 0 | Required and adjacent regressions |
| `...python -m pytest tests -o addopts='' -q` final | **453 passed in 3.04s**, exit 0 | Full Python suite, 44 above baseline |
| Required outbound `rg` audit plus broader SDK/HTTP audit | Nine inference, two embedding and one search caller accounted for; exit 0 | [P01 outbound map](../evidence/P01-outbound-map.md) |
| Independent code review and focused re-reviews | Three filing findings fixed with failing regressions first; final review found no remaining actionable issue in fixes and independently reran **110 outbound/Filer tests**, all passed | Group preservation, sanitized title mapping and collision regressions |
| `git diff --check`, `git diff --cached --check` | Clean, exit 0 | Implementation commit |

## State and recovery

- Schema/configuration changes and migrations exercised: index metadata now has `outbound_version: 1` and `notes`. Old unmarked caches cannot provide search/glimpses/reused embeddings even if modification dates match. Rebuild retains legacy JSON/NPY locally as `.unprepared`, then writes prepared content. Synthetic regression exercised exclusion/rebuild; no real cache migration was run. `filer.Item` carries source ID/timestamp into local pending-item records; older records without IDs cannot be used for outbound classification. Policy schema and model configuration are unchanged.
- What is running and how to stop it: no app, listener or service started; existing processes untouched. Temporary synthetic pytest data/logs are disposable.
- External services touched: none. Only local filesystem, Git and Python test execution.
- Backup / rollback procedure exercised: Task 1 policy atomic-write/recovery tests remained green. New synthetic test confirms unmarked index exclusion and sanitized rebuild; this is not encrypted migration. Code rollback can revert `e37bd3d` and the subsequent roadmap commit on this branch. Do not run the reverted raw-input code on real data. Original checkout and Apple data require no restoration because they were never changed.
- Remaining risks or failed cases: no known failing Task 2 regression. Supported-pattern redaction is imperfect by design; it does not detect arbitrary secrets. Current and quarantined caches remain plaintext pending Task 3. Revocation filters future use but persistent purge/encryption/retention remain Task 3. Existing model-generated citation HEAD checks and endpoint overrides remain Task 4. Provider retention, native signing/Keychain/permission behavior, and managed-service enforcement were not verified. Process-local capabilities and non-atomic Apple writes remain P02 limitations. No production release or P01-wide exit gate is claimed.

## Next session

- Exact next task and first command: **P01 Task 3 — Sensitive cache migration and retention**. First run `git status --short` from `/Users/m1labs/Dev/apps/juno/.worktrees/p01-task1`, then inspect branch/recent commits and read this handoff, `CLAUDE.md`, production README/design and P01 Task 3. Continue using `/Users/m1labs/Dev/apps/juno/.venv/bin/python -m pytest ...`.
- Inputs that block only that task: none for synthetic implementation and injected in-memory credential/store tests. Real-data processing/native Keychain validation still depends on the signed helper/P06 integration; do not use real credentials or caches to work around it.
- Decisions made, with links to updated contracts: local `PolicySnapshot` remains authoritative; `model` and `diagnostic` explicitly identify proposal/measurement origins without authority; note source metadata remains attached until each boundary; plaintext legacy index is excluded until rebuilt; ambiguous sanitized filing labels are refused. See [Task 2 implemented contract](../design.md#p01-task-2-implemented-outbound-contract-2026-09-04) and [complete caller map](../evidence/P01-outbound-map.md).
- Roadmap checkboxes changed and evidence supporting each: all seven Task 2 checklist items checked based on observed failing regressions, complete caller conversion/audit, malicious-content fixtures, independent review, and 453 passing tests. P01 status is **in progress — Tasks 1–2 complete; Task 3 next**. Task 3–5 checkboxes, M1 and P01 exit gate remain unchecked.
