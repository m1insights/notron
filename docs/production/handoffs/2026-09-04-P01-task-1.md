# Session handoff

## Work identity

- Plan and task IDs: **P01 Task 1 — explicit policy state and zero-home semantics**, complete. No later task started.
- Branch/worktree and commit: `production/p01-task1`, `/Users/m1labs/Dev/apps/juno/.worktrees/p01-task1`; implementation and contract commit **`2b3914d`**. This handoff and roadmap updates are in the subsequent documentation commit on that branch. Base: `759d8ae`, `fix/folder-index-drift`. Branch retained locally; no merge or push.
- Starting workspace changes not owned by this task: modified `notron.egg-info/PKG-INFO`, `notron.egg-info/SOURCES.txt`; untracked `.claude/`, `docs/plans/2026-09-03-pin-her-notes.md`, `notrontask.md`. Preserved in the original checkout; final original-checkout status matches the starting status. No previous production handoffs existed.
- Implemented behavior: versioned, validated `PolicySnapshot`; missing/unconfigured versus corrupt state; zero automatic destinations for zero homes; unknown-note reads off by default; Ignore and sensitive-title restrictions retained; current watcher capability permits one read-only reply bound to a stable note ID; filing and reply destination identity checks at execution; read-only cannot exercise rewrite permission; strict rewrite validation; atomic persistence, validated backups and explicit recovery; setup registers system-note IDs without enabling AI on a fresh install; Mac saves through Python stdin transport. Explicitly approved new-note creation adds that new note as a Home even from zero homes. Existing legacy selections are recognized without silently approving other notes.
- Explicitly not implemented: P01 Task 2 input provenance/redaction transport, Task 3 encrypted caches/Keychain migration, Task 4 network restrictions, Task 5 security inventory; P02 durable request/operation records and revision-safe recovery; native release/onboarding work in P06. No model/provider changes.
- Stack: Python 3.11+ project (tests ran under Python 3.14.2), pytest, Swift/SwiftUI macOS companion. Existing Nebius model configuration unchanged: `nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B` routing/scheduling, `nvidia/nemotron-3-super-120b-a12b` writing/filing, `Qwen/Qwen3-Embedding-8B` embeddings. No provider calls used for verification.

## Evidence

All Python tests used synthetic data and mocked Apple integrations. No real Notes, Reminders, Calendar, provider credentials or network traffic were used. The app was compiled, never launched. Commands ran in the isolated worktree. During testing, `.venv` was a temporary symlink to the original checkout's existing environment; it was removed after verification. Use `/Users/m1labs/Dev/apps/juno/.venv/bin/python` for subsequent worktree tests.

| Command or manual case | Actual result / exit code | Artifact |
|---|---|---|
| Original-checkout `git status --short`, `git branch --show-current`, `git worktree list` | Unrelated changes identified; branch `fix/folder-index-drift`; exit 0 | Starting changes above |
| Baseline `/Users/m1labs/Dev/apps/juno/.venv/bin/python -m pytest tests -q` | 371 tests passed; exit 0 | Baseline on `759d8ae` |
| Initial new policy/persistence test collection | Expected missing-module errors; exit 2 | New modules did not yet exist |
| `...python -m pytest tests/test_library.py -q -k 'zero_homes_has or missing_policy_does'` before implementation | Both regressions failed: readable notes became filing candidates and missing policy exposed notes; exit 1 | `tests/test_library.py` |
| Executor, wrong-twin reply, unregistered care read, unsupported rewrite version, organizer permission-copy regressions before their fixes | Each failed for its intended unsafe behavior; exit 1 | `tests/test_policy.py` |
| `.venv/bin/python -m pytest tests/test_policy.py tests/test_library.py tests/test_filer.py tests/test_persistence.py -o addopts='' -q` | **138 passed**, exit 0 | Task-required targeted suite |
| `.venv/bin/python -m pytest tests -o addopts='' -q` after final code change | **409 passed in 3.05s**, exit 0 | Full regression suite |
| `swift build --package-path mac --scratch-path /tmp/notron-p01-swift-build` | Initial sandbox failure accessing compiler cache; approved rerun **build complete**, exit 0 | Swift 5 language mode, macOS 26.2 SDK, arm64 macOS 14 target |
| Focused independent code review and re-review | Four findings fixed; final focused re-review found no important Task 1 blocker; 182 synthetic tests passed | Regressions for care, duplicate reply identity, invalid rewrite version, read-only organizer |
| `git diff --check` | Clean, exit 0 | Final code and contract diff |

Swift emitted pre-existing captured-`self` concurrency warnings in `Onboarding.swift` and unchanged sections of `Library.swift`; they become errors under Swift 6 language mode. Native permission prompts, signing and real-device behavior were not tested.

## State and recovery

- Schema/configuration changes and migrations exercised: legacy Library JSON read without rewriting; saved wrapper adds `version: 1`, `allow_new_notes`, `system_notes`. Empty `chosen_at` with no user selections remains unconfigured even after setup records system IDs. Valid empty selection is ready with zero note grants. `start_from` cannot admit unknown notes unless explicitly enabled. Rewrite JSON validates version 1 and recognized legacy shape. Mac `library save` preserves system IDs and settings it does not expose. See [implemented contract](../design.md#p01-task-1-implemented-policy-contract-2026-09-04).
- What is running and how to stop it: no listener, app or service started. Swift output is a disposable build under `/tmp/notron-p01-swift-build`. Existing processes were not modified.
- External services touched: none. Git worktree creation, commit metadata and Swift compiler cache access required filesystem escalation; no deployment or account changes.
- Backup / rollback procedure exercised: write 0600 same-directory temporary file, flush/fsync, replace, fsync directory. Serialization failure and replacement interruption preserve the original. Previous validated policy retained as `.bak`; corrupt reads do not restore it. `notron library recover` explicitly restores a validated backup and preserves displaced bytes in `.corrupt`; invalid backup is refused. `notron library --reset` explicitly clears grants. `notron rewrite --recover` uses equivalent validated recovery. Automated tests exercised library and rewrite recovery with synthetic state only. For code rollback, revert implementation commit on the task branch; do not manually copy a permissive backup into a running installation.
- Remaining risks or failed cases: no known failing Task 1 regression. Legacy installations need explicit setup to register system IDs; duplicate system titles stop setup. A deleted/unregistered Log is not recreated by title, so setup is needed to restore that Notes audit destination. P01-wide privacy claims remain unearned until outbound preparation, encryption and network work finish. Concurrent external edits between the final check and Apple write remain a P02 limitation; capabilities are process-scoped, not durable. No real-data migration or native signing/permission validation performed. Swift concurrency warnings remain P06 work.

## Next session

- Exact next task and first command: **P01 Task 2 — All model/search inputs carry provenance**. From `/Users/m1labs/Dev/apps/juno/.worktrees/p01-task1`, first run `git status --short`, then read this handoff, `CLAUDE.md`, `docs/production/design.md`, and `docs/production/plans/01-privacy-permissions.md`. Use `/Users/m1labs/Dev/apps/juno/.venv/bin/python -m pytest ...` for tests.
- Inputs that block only that task: none for synthetic implementation/tests. Real data/provider processing must wait for the later secure-storage and native integration gates.
- Decisions made: policy preserves recognized legacy choices without the old all-readable fallback; default new-note reads off; one explicitly approved new note becomes a Home; watcher capabilities never come from model text; system IDs are registered during explicit setup; Mac saves share Python validation/persistence. `PolicyError` is owned by `policy.py`; Task 2 should import/re-export it instead of creating a competing exception. All recorded in the shared contract above.
- Roadmap checkboxes changed and evidence supporting each: all five P01 Task 1 checklist items checked based on red/green regression evidence, atomic/recovery tests, full Python suite and Swift build. P01 status is **in progress — Task 1 complete; Task 2 next**. M1 and the P01 exit gate remain unchecked. No Task 2–5 checkbox changed.
