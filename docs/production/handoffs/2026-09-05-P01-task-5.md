# Session handoff

## Work identity

- Plan and task IDs: **P01 Task 5 — Security inventory and release-facing assertions**, authorized implementation complete. P01 Tasks 1–5 local implementation complete; public reporting/native/release gates remain open. **P02 not started.**
- Branch/worktree and commit: `production/p01-task1`, `/Users/m1labs/Dev/apps/juno/.worktrees/p01-task1`. Started clean at `1a31b8a`, following Task 4 implementation `5649a71`. Task 5 code, tests, CI definition, security documentation, shared contract and audit artifacts: **`40097bf`**. This handoff and roadmap are the following documentation commit. Keep branch/worktree; no merge, push or publication.
- Starting workspace changes not owned by this task: none in the isolated worktree. Original checkout still contains unrelated `notron.egg-info/PKG-INFO`, `notron.egg-info/SOURCES.txt`, `.claude/`, `docs/plans/2026-09-03-pin-her-notes.md`, and `notrontask.md`. No original-checkout files were edited, staged or merged.
- Implemented behavior: scheduler rejects nontext fields before constructing existing Action types; dynamic Calendar/Reminders JXA carries one JSON argv object into fixed source; launchd plists use structured serialization so paths cannot inject XML/arguments. New production-boundary security regressions, strengthened helper argv assertions, global unmocked-subprocess denial alongside unchanged DNS/socket guards, and a frozen-dependency synthetic CI workflow. SECURITY.md and the security report distinguish implemented protections from threats/future work; README/CLAUDE security/privacy claims corrected. Complete HTTP/subprocess inventory and lockfile advisory audit recorded.
- Explicitly not implemented: P02 request ledger/revision/retry/undo work; native Swift/JXA execution validation; signed Keychain startup/permission/installer/updater integration; private reporting channel; provider retention agreement; hosted service; real processing, migration, listener/helper/launchd startup; hosted CI execution; deployment, merge or push.
- Stack: Python 3.11+ project, Python **3.14.2** tests, Swift/macOS, AppleScript/JXA/EventKit, existing AES-GCM storage. `openai==3.7.0`, `httpx2==2.12.0`, `cryptography==50.0.1` and all locked application dependencies unchanged. Nebius models unchanged: routing/scheduling `nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B`; writing/planning/filing `nvidia/nemotron-3-super-120b-a12b`; configured deep tier `nvidia/Nemotron-3-Ultra-550b-a55b`; embeddings `Qwen/Qwen3-Embedding-8B`. Tavily search unchanged. No actual model calls.

## Evidence

All pytest commands ran in the isolated worktree using
`/Users/m1labs/Dev/apps/juno/.venv/bin/python -m pytest` (abbreviated below).
No worktree-local `.venv` was created and no package was installed into the shared
root test environment. Tests used synthetic temporary files and mocked Apple,
Keychain/helper, inference, embedding, search, HTTP/DNS and subprocess adapters.
No real Notes, Reminders, Calendar, credentials, Keychain entries or private caches
were read or modified. No live provider/citation requests or unmocked test DNS.

| Command or manual case | Actual result / exit code | Artifact |
|---|---|---|
| Initial branch/status/log and linked-worktree check | Clean `production/p01-task1`, HEAD `1a31b8a`; linked worktree verified; exit 0 | Work identity |
| Baseline `pytest tests -o addopts='' -q` | **624 passed in 4.52s**, exit 0 | Task 4 baseline preserved |
| New `test_security_boundaries.py` before fixes | **10 failed, 23 passed in 0.62s**, exit 1; five schema, three JXA-data, two plist failures | A first draft also contained an incorrect `notes.show` test name, corrected before this red run |
| New boundary suite after fixes | **33 passed in 0.53s**, exit 0 | Initial boundary cases |
| First full run after JXA change | **4 failed, 653 passed**, exit 1; old date-in-source assertions obsolete | Updated calendar/reminder tests now assert data argv fields |
| Final `pytest tests/test_security_boundaries.py -o addopts='' -q` | **40 passed in 0.62s**, exit 0 | Includes additional writer/permission/reader/startup coverage |
| Final `pytest tests/test_privacy.py tests/test_policy.py tests/test_outbound.py tests/test_network.py tests/test_credentials.py tests/test_graph.py tests/test_executor.py tests/test_actions.py tests/test_calendar.py tests/test_reminders.py tests/test_eventkit.py tests/test_citations.py tests/test_securestore.py tests/test_cache_migration.py tests/test_library.py tests/test_filer.py tests/test_undo.py tests/test_watch.py -o addopts='' -q` | **474 passed in 2.65s**, exit 0 | Adjacent policy/privacy/outbound/network/credential/graph/executor/storage/recovery coverage |
| Final `pytest tests -o addopts='' -q` | **664 passed in 3.28s**, exit 0; 40 above baseline | Full Python suite |
| Independent read-only code review | No blocking findings; independently reproduced **664 passed in 2.80s** and **66 targeted passes** | Native execution remains explicitly unverified; existing CLI option issue recorded below |
| Independent read-only security-doc/audit review | Local completion vs release gates clear; both inputs/results exactly cover all 26 locked registry version pairs | Remaining legacy overstatements corrected after checking actual code |
| `uv lock --check --offline` (uv 0.9.22) | Exit 0, 27 package records including local Notron | uv.lock unchanged |
| `pip-audit==2.10.1`, `--no-deps --disable-pip --strict`, PyPI advisory service | Two completed runs, each **25 packages / 0 known vulnerabilities / 0 skipped**, exit 0; union **26 distinct locked name/version pairs** | [Commands, tool environment, raw results, scope and triage](../evidence/P01-security-boundaries.md#dependency-vulnerability-audit) |
| Initial combined audit input | Exit 1 for duplicate NumPy requirements; no completed audit claimed | Split locked NumPy 2.4.6 (<3.12) and 2.5.2 (>=3.12) inventories, preserving all entries |
| Stdlib verification of lock/input/result sets and lock SHA256 | All 26 registry pairs covered exactly, none skipped/advisories; exit 0 | Committed `P01-dependency-audit` artifacts |
| Complete HTTP/process source audit | Four application HTTP operations unchanged; **10 Python subprocess call sites / one Swift Process boundary**, no shell/model-code path found | [Outbound/subprocess map](../evidence/P01-outbound-map.md) |
| CI YAML parsed with Ruby YAML; action SHAs checked with public `git ls-remote` | Exit 0 | Definition only; not a remote CI run |
| `git diff --check`, `git diff --cached --check` | Exit 0 before implementation commit | Task-owned changes only |

Audit triage: no returned vulnerability IDs required severity assignment, suppression,
mitigation or upgrade. This does not cover unpublished vulnerabilities, package
malice, interpreter/OS/build dependencies or a future shipped bundle. `requirements.txt`
is historical/incomplete; locked development installs use pyproject + uv.lock.
Setuptools build requirement remains a lower bound outside the audited lock; P06/P07
must inventory/pin shipped artifacts and repeat the audit. No silent upgrades.

## State and recovery

- Schema/configuration changes and migrations exercised: no policy/storage schema or migration change. EventKit's existing adapter now supports `data=` for dynamic calls, passed as JSON argv; calendar/reminder test callers consume that keyword. No new policy, provenance, credential or network definitions. Launchd serialization changes representation, not job names/commands/log destinations. CI is newly defined because no tracked configuration existed.
- What is running and how to stop it: no listener, app, Keychain helper, launchd job or service was launched. Test/audit processes exited. Task-owned disposable audit environment and public metadata cache were removed after evidence was copied. Existing processes were untouched.
- External services touched: public PyPI/auditor-package/advisory downloads; public GitHub pip-audit documentation and read-only checkout/setup-python tag references. No provider or citation requests, private reporting contact, publishing, or private account action. Unit tests retained DNS/socket blocks throughout.
- Backup / rollback procedure exercised: no real migration or user-data rollback was performed. Existing synthetic migration/encryption/undo tests remain green. Revert the Task 5 implementation and documentation commits to roll back code/docs if necessary; doing so does not authorize real processing or remove the P06 gate. Do not overwrite unrelated original-checkout work.
- Remaining risks or failed cases: no failing final Python tests or blocking Task 5 review finding. Native JXA function/argv behavior, Swift Core and signed Keychain integration were not executed/verified. Existing AskNotronIntent passes a single request without `--`, so `--help` can select help rather than request text; no arbitrary shell/operation route results, and P06 should fix/test native CLI argument handling. Local PATH/dev runtime/helper paths remain trusted mutable configuration. Compromised-account access, imperfect redaction, untrusted model instructions and unverified provider retention remain limits. Notes replace/restore races, rich-content loss, retry ambiguity and durable undo remain P02 work. No sandbox/production-readiness claim.

## Next session

- Exact next task and first command: **P02 Task 1 — Request envelopes and durable operation state.** First run `git status --short` in `/Users/m1labs/Dev/apps/juno/.worktrees/p01-task1`, then inspect branch/recent commits and read CLAUDE.md, production README/design, this handoff, SECURITY.md, P01 security evidence and `docs/production/plans/02-reliable-execution.md`. Reuse the root venv; baseline **664 passing Python tests**. Keep branch/worktree; do not merge/push without a new instruction.
- Inputs that block only that task: none known for synthetic P02 ledger/request tests. Private vulnerability reporting requires an owner-chosen, verified route **before public release**, not before P02 implementation. No recipient was invented/contacted and remote private-report settings are unverified. P06 signed/native startup stays blocked; do not bypass it for tests.
- Decisions made, with links to updated contracts: [Task 5 shared contract](../design.md#p01-task-5-implemented-security-boundary-contract-2026-09-05), [security report](../evidence/P01-security-boundaries.md), [SECURITY.md](../../../SECURITY.md). Keep code-controlled operations; JSON argv for dynamic JXA; structured launchd arguments; conservative lock-union audit; explicit local-code vs native/public-release distinction.
- Roadmap checkboxes changed and evidence supporting each: four Task 5 implementation items checked; P01 marked local implementation complete. M1's P01 regression and existing-test items checked on **664 passes**; M1 still requires P02/P03. M2/M3 remain open, with an explicit private-reporting/provider-retention/shipped-inventory item added to M3. No native, hosted-service, pilot or public-release gate is marked complete.
