# Session handoff

## Work identity

- Plan and task IDs: **P01 Task 3 — Sensitive cache migration and retention**, implementation complete. **Task 4 not started.** Signed/native Keychain startup remains a P06 gate; no production-readiness claim.
- Branch/worktree and commit: `production/p01-task1`, `/Users/m1labs/Dev/apps/juno/.worktrees/p01-task1`. Started clean at `bacdf54`, following Task 2 `e37bd3d`. Implementation/tests/shared contract/evidence: **`c5801df`**. This handoff and roadmap are the following documentation commit. Worktree and branch retained; no merge or push.
- Starting workspace changes not owned by this task: none in this worktree. Known original-checkout changes remain: `notron.egg-info/PKG-INFO`, `notron.egg-info/SOURCES.txt`; untracked `.claude/`, `docs/plans/2026-09-03-pin-her-notes.md`, `notrontask.md`. None was staged, overwritten, or merged.
- Implemented behavior: injected `CredentialStore` with allowlisted names, Swift Security/LAContext bridge source and dedicated inherited process pipe; no credential `.env` or environment fallback; native startup deliberately paused pending P06. AES-GCM encrypted index/vectors, lossless single-slot undo, filing request/proposal state and reflection records. Managed Application Support content/metadata paths, private atomic files/directories, fixed seven-day diagnostic/usage counters. Explicit offline initialize/migrate/accept CLI/API; validated JSON/NPY/.unprepared import, encrypted exact backups, no raw vector reuse, verified resumable publication and acceptance, originals/backups retained until separate acceptance. Policy/inventory cleanup runs before manual and listener processing and semantic search; renamed sensitive, ignored/deleted content and listener pending memory purged. Credential/storage/policy errors cannot become model fallbacks. Tasks 1–2 policy/provenance protections remain intact.
- Explicitly not implemented: Task 4 URL/provider-destination constraints, Task 5 vulnerability audit/security inventory, P02 durable transactions/concurrency, signed/native helper packaging/identity/Keychain tests, credential setup/removal UI, automatic key rotation, real-data migration, deployment, or provider-retention verification.
- Stack: Python 3.11+ project, **Python 3.14.2** for tests; `cryptography==50.0.1` AESGCM, NumPy in-memory vectors; Swift Security and LocalAuthentication source, **Apple Swift 6.2.3**, arm64 macOS 26 target. Nebius remains the inference provider; exact defaults unchanged: `nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B`, `nvidia/nemotron-3-super-120b-a12b`, `nvidia/Nemotron-3-Ultra-550b-a55b`; embeddings `Qwen/Qwen3-Embedding-8B`. No actual model calls.

## Evidence

All tests used synthetic temporary files and mocked Apple, Keychain, inference,
embedding and search integrations. No real Notes, Reminders, Calendar, credentials,
Keychain entries or private caches were read or modified. Swift was type-checked,
not launched. The provided root venv was used throughout; no worktree-local `.venv`
was created. Public cryptography documentation/PyPI and dependency downloads were
accessed; no private content or credentials were sent.

All pytest commands below use `/Users/m1labs/Dev/apps/juno/.venv/bin/python` and
ran in the task worktree.

| Command or manual case | Actual result / exit code | Artifact |
|---|---|---|
| Initial branch/status/recent commits | Clean `production/p01-task1`, HEAD `bacdf54`; exit 0 | Work identity |
| `...python -m pytest tests -o addopts='' -q` baseline | **453 passed in 3.06s**, exit 0 | Task 2 baseline |
| Initial securestore/credentials/migration regressions, before implementation | **16 failed in 0.20s**, exit 1 | Three new test modules |
| Subsequent required failing regressions | Locked/legacy bypass, migration interruption/gate, schema/mode/retention, direct API/cleanup, sensitive-title/memory purge, initialization/acceptance corruption all failed before their fixes | [Storage evidence](../evidence/P01-secure-storage.md) |
| `...python -m pytest tests/test_securestore.py tests/test_credentials.py tests/test_cache_migration.py tests/test_privacy.py -o addopts='' -q` final | **50 passed in 0.26s**, exit 0 | Required Task 3/privacy suite |
| `...python -m pytest tests/test_securestore.py tests/test_credentials.py tests/test_cache_migration.py tests/test_privacy.py tests/test_policy.py tests/test_outbound.py tests/test_research.py tests/test_graph.py tests/test_library.py tests/test_filer.py tests/test_reflect.py tests/test_care.py tests/test_nodes.py tests/test_watch.py tests/test_undo.py tests/test_executor.py tests/test_persistence.py -o addopts='' -q` final | **355 passed in 2.96s**, exit 0 | Targeted and adjacent regressions |
| `...python -m pytest tests -o addopts='' -q` final | **489 passed in 3.76s**, exit 0; 36 above baseline | Full Python suite |
| `swiftc -typecheck -D NOTRON_KEYCHAIN_HELPER -parse-as-library mac/Sources/Notron/KeychainStore.swift` | Exit 0, no warnings | Bridge source; not native Keychain validation |
| `uv lock --check` | Exit 0; 27 packages resolved, pinned lock valid | `pyproject.toml`, `uv.lock` |
| Independent review and focused re-reviews | Migration/cleanup findings fixed with regressions; final focused review found no actionable issues and reran **36 tests, all passed** | New migration/credential/storage tests |
| `git diff --check`, `git diff --cached --check` | Exit 0, clean | Implementation commit |

## State and recovery

- Schema/configuration changes and migrations exercised: `.enc` envelope version 1 binds logical filename/version with AES-GCM; index keeps `outbound_version: 1` and vectors in note rows. `read_json`/`write_json` enforce migration/key readiness. Content uses Application Support; policy/rewrite paths and authority remain unchanged. Migration manifest/journal version 1 authenticates source and output hashes. Synthetic legacy normal/prepared/quarantined inputs exercised; no real migration. Filing/reflection legacy state is invalidated and recomputed; approved undo bodies round-trip exactly. No automatic policy or credential migration.
- What is running and how to stop it: no app, helper, listener or launchd service was started. Existing processes untouched. Tests/compiler exited; temporary synthetic pytest data can be discarded.
- External services touched: public documentation/PyPI for AES-GCM and pinned dependency installation only. `cryptography 50.0.1`, `cffi 2.1.1`, `pycparser 3.0` were installed into the requested shared root venv. No Nebius/Tavily/Apple/Keychain calls.
- Backup / rollback procedure exercised: interrupted atomic writes preserve prior ciphertext; migration resumes after output or manifest/status interruption. Separate acceptance verifies originals, encrypted backups and output hashes before retiring originals/recovery copies; interrupted acceptance refuses damaged active output and preserves remaining backups. Direct cache consumers and missing-sidecar paths cannot bypass acceptance. Lost/rotated wrong key pauses; restoring the exact original synthetic key restores access. Code rollback can revert `c5801df` and the following documentation commit, but reverted plaintext code must not process real data or encrypted caches. Old-format data is never silently restored. Acceptance uses ordinary durable unlink, not forensic erasure.
- Remaining risks or failed cases: no known failing Task 3 regression. P06 must supply signed packaging/identity validation and wire the native provider at startup; default startup intentionally refuses real protected work today. Native locked/unlocked behavior, installed accounts, OS backups/snapshots, a compromised local account and memory wiping are unverified. Lost keys cannot recover encrypted data. No automatic rotation is implemented. Cache updates remain per-file atomic, not cross-process transactions (P02). Pattern redaction remains imperfect. Task 4 model-generated URL checks/endpoint restrictions and Task 5 pinned dependency audit remain outstanding. Provider retention is unverified.

## Next session

- Exact next task and first command: **P01 Task 4 — Restrict outbound URL validation and provider destinations.** First run `git status --short` from `/Users/m1labs/Dev/apps/juno/.worktrees/p01-task1`, then inspect branch/recent commits and read this handoff, `CLAUDE.md`, the production README/design, P01 plan and outbound caller map. Use the same root venv for tests. Baseline now **489 passing Python tests**.
- Inputs that block only that task: none for synthetic URL/resolver/transport tests. Do not bypass the P06 secure-startup gate or use real credentials to test network restrictions.
- Decisions made, with links to updated contracts: pending migration blocks every protected/direct cache path; backups are encrypted exact original bytes and retained until explicit acceptance, then retired; index vectors are rebuilt from approved notes rather than reused from legacy files; mixed-source filing/reflection state is invalidated conservatively; no automatic key fallback; metadata inventory governs deletions and current sensitive titles. See [Task 3 shared contract](../design.md#p01-task-3-implemented-secure-storage-contract-2026-09-04) and [storage inventory/evidence](../evidence/P01-secure-storage.md).
- Roadmap checkboxes changed and evidence supporting each: six Task 3 implementation items checked based on observed failing regressions, 489 passing tests, authenticated migration/cleanup evidence, Swift type-check and independent review. Native signed Keychain integration explicitly remains P06. P01 status is **Tasks 1–3 complete; Task 4 next**. Tasks 4–5, M1, P01-wide exit gate and native release gates remain unchecked.
