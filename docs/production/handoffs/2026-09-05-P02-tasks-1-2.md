# P02 Tasks 1–2 — batch handoff

Date: 2026-09-05. Authorized scope: P02 Tasks 1 and 2 only.
Status: both tasks complete; individual and final batch reviews approved with no
remaining Important findings. Tasks 3–6 and the full P02 exit gate remain open.

## Outcome

- Task 1 persists request identity before inference, tracks separate occurrences,
  and stores request/operation state in SQLite with encrypted content. Interrupted
  or ambiguous work pauses for review; missing ledger history cannot silently reset
  identity. IDs carry through graph, Ask, mentions, Brain Dump and CLI filing.
- Task 2 targets existing Notes by ID, captures revisions before inference, checks
  source/target/policy again under a local write lock, and verifies the resulting
  body. Stale replacements refuse; append/insert requires safe anchor matching.
  Approved creation checks its persisted sources and approval line. Unsupported
  rich originals stay untouched; separate results go to a guarded existing Ask
  note, or leave the request needing review when delivery is unsafe.
- Stack: Python 3.11+ (tested with Python 3.14.2), stdlib SQLite, existing AES-GCM
  encrypted storage and AppleScript/JXA/EventKit adapters. Swift/SwiftUI unchanged;
  no dependencies or AI provider/model settings changed. No provider calls ran.

Detailed contracts: [shared design](../design.md). Historical task evidence:
[Task 1](2026-09-05-P02-task-1.md), [Task 2](2026-09-05-P02-task-2.md).
This batch handoff supersedes their interim next-step and verification status.

## Location and commits

Keep branch `production/p01-task1` and worktree
`/Users/m1labs/Dev/apps/juno/.worktrees/p01-task1` for the next session.
The original checkout and its unrelated changes were left untouched.

| Checkpoint | Commit |
|---|---|
| Starting P01 baseline | `a3361eaa846e2b329c5c7b83a0a7bf2333609333` |
| Task 1 implementation | `ebcca8d52d02287680912da4054eca6f22ffcbf9` |
| Task 1 first review fixes | `dea6adb226a8db4b7da2fc2c3e6235660f09ff27` |
| Task 1 final approved fixes | `08d2a6df1fc057ff5303a752360804d6b9b9e8b1` |
| Task 2 implementation | `1487fff822249bdf2c5314be57d3d3e261b2fc1c` |
| Task 2 approved review fixes | `a09883d7f9c33b074c8c60319d7d827c2f877dcb` |
| Consolidated integration fixes | `6c6492c230d44e3c49208693ee922e066c94ae4e` |
| Receipt provenance correction | `9d9b720e7a4ae96775e2c9d282dfddd8cb0c88f4` |

No merge, push, deployment, native app launch or real user-data migration occurred.

## Verification and review

Root verification at `9d9b720`, from the worktree:

```sh
/Users/m1labs/Dev/apps/juno/.venv/bin/python -m pytest tests -o addopts='' -q
git diff --check
```

- **798 passed in 8.01s**, exit 0 (134 more than the 664-test P01 baseline).
  No exclusions; Task 2's previously pending race suite is now included.
- Whitespace check exit 0. Tests use synthetic Notes, temporary encrypted stores,
  fake credentials, and network/subprocess denial. No live Apple data or AI calls.
- Task 1 independent review approved after regressions fixed uncertain requests
  disappearing/reappearing, lost-ledger identity reset, and duplicate occurrence
  mapping through receipts and unrelated edits.
- Task 2 independent review approved after regressions fixed stale proposal-source
  copying during approved creation and invisible rich-note results in listener use.
  Its fix suite added 13 cases; focused filer/write-race suites passed 132 tests.
- Final batch integration review found two cross-task gaps: late source-policy
  invalidation could allow a write, and deletion retention omitted secondary
  sources from operation payloads. Three synthetic probes reproduced these issues.
  The consolidated correction adds complete immutable content-source provenance,
  checks source permissions before encrypted preparation and before mutation, and
  verifies that the operation is still APPLYING. Ten new regressions passed after
  failing before their fixes; 202 focused tests passed. Scoped review confirmed
  those cases, then identified destination/proposal text omitted from filing
  receipt provenance. A focused follow-up corrects normal, approval, creation,
  decline and failed-creation receipt contributors. Six new cases failed before
  correction; all seven added cases pass, including the legacy-approval control.
  The focused suite passed 195 tests; root's final full suite passed 798 tests.
  Independent narrow final confirmation approved the correction and the complete
  local Tasks 1–2 batch. No remaining Important findings or deferred review issues.
  Approval applies to this local implementation, not a product release.

## Scope decisions and remaining limits

1. The existing approved plan plus the user's instruction authorized implementation
   through Task 2. No new design approval was introduced. If that scope assumption
   is wrong, the local commits can be revised; nothing was published.
2. Automatic Brain Dump and CLI filing receive request admission now because they
   bypass graph admission. Care/reflection/index maintenance orchestration remains
   Task 6; their Notes writes are guarded by Task 2. Morning request provenance is
   preserved. The cost of deferral is that maintenance jobs still lack the complete
   durable lifecycle until Task 6.
3. Hidden automatic AppleScript retries were disabled for Notes mutations because
   retries bypass the ledger and revision checks. The tradeoff is more visible
   uncertain outcomes after timeouts; Task 3 owns reconciliation.
4. The final review identified one remaining receipt-provenance omission. It was
   kept in this batch despite the workflow skill's final correction-cycle cap: the
   higher-priority instruction requires finishing authorized work, and a known
   P01/P02 privacy gap would leave it incomplete. The cost was an additional
   focused correction and review; no Task 3 recovery work was introduced.

- Local locking cannot make iCloud writes atomic. A remote edit in the final
  read/write gap can still be lost without detectable post-write divergence; the
  synthetic suite demonstrates this limit. No exactly-once claim is made.
- Native Notes metadata/HTML normalization, rich-content fidelity and multi-device
  behavior still need native validation. Conservative HTML checks may refuse
  unfamiliar ordinary formatting. P06 secure signed startup remains pending;
  real processing stays paused.
- Task 3 owns reminder/event reconciliation, receipt-only recovery, filing copy/mark
  suboperations and independent audit recovery. APPLIED records verified Notes body
  evidence; it is not proof that a final receipt was delivered.
- Task 4 owns durable undo peek/verify/consume and snapshot provenance. The current
  undo slot still pops before restore and may be replaced before a later refusal.
- Task 5 owns date/target/context clarification. Task 6 owns worker lifecycle,
  maintenance admission, legacy migration and paused-request resolution.
- SQLite schema is now 3, recording complete operation content provenance. Upgrade
  from schemas 1/2 removes legacy operation payloads that lack complete provenance
  and retains IDs/history; unfinished affected work needs review. Keep ledger and
  encrypted payloads as a matched backup. Older code refuses schema 3; rollback
  requires compatible code or an explicit matched restore. Never delete identity/
  tombstone files to force retry.
- P01 reporting-route, provider-retention and P06/P07 release gates remain open.

## Next session

Continue with **P02 Task 3 — Side effects and receipts recover independently**.
Read the production README, shared design, P02 plan, CLAUDE.md and this handoff.
First inspect the retained worktree:

```sh
git -C /Users/m1labs/Dev/apps/juno/.worktrees/p01-task1 status --short --branch
```

Reuse the root venv and synthetic test guards. Start with the planned boundary
failpoint harness for fake reminder/event/Notes stores, using the real graph and
operation ledger. Preserve Tasks 1–2's identity, policy, source and revision guards.
Do not treat request admission or APPLIED as complete cross-effect recovery.
