# P02 Task 2 — stable targets and revision-aware writes

Status: implementation complete, ready for independent review. Task 1 independent
review approved baseline `08d2a6df1fc057ff5303a752360804d6b9b9e8b1` before this task.
Branch `production/p01-task1`; worktree
`/Users/m1labs/Dev/apps/juno/.worktrees/p01-task1`. The task-owned commit contains
this report's tracked counterpart at `docs/production/handoffs/2026-09-05-P02-task-2.md`;
resolve SHA with `git log -1 --format=%H -- docs/production/handoffs/2026-09-05-P02-task-2.md`.

## Outcome and contract

- ID-bound `Write`/`capture_write`/`Executor.apply_write`, with SHA-256 revision
  captured before inference. Organizer, planner, watcher/reply/Memory, care,
  reflection and filing use guarded targets. No model executes in the write path.
- Reentrant local thread/process transaction lock; immutable encrypted operation
  payload, required encrypted backup, final target/source/revision/policy checks,
  post-write read and durable observed revision. Unknown write outcomes pause for
  review; same operation cannot blindly reapply. Notes mutation adapters have no
  hidden timeout retry. Explicit approved creation never appends to a title twin.
- Stale replace/restore refuses. Stale append/insert/mark requires unambiguous
  captured anchors; captured positions disambiguate only unchanged bodies. Journal
  append layout never rebases. Filing holds source IDs/revisions through copy/tick,
  checks changed/deleted/ambiguous source lines before copying, excludes duplicate
  master titles, and reclassifies legacy title-only cached destinations.
- Conservative supported HTML validation refuses rich replacements; organizer
  returns separate text without writing the rich original. Failed writes cannot
  leave organizer/undo success wording. Audit uses the same guarded path and
  preserves verified primary success even when logging fails.
- SQLite schema 2 atomically migrates v1 with nullable observed_revision, retaining
  identity, encrypted payloads, history markers and tombstones. `finish` preserves
  a request already marked needs_review by approved creation's add_home policy
  update; no purge/revocation relaxation or replay reset.
- Stack: Python 3.11+ (verified with existing Python 3.14.2 root venv), stdlib
  SQLite/threading/fcntl, existing encrypted store, AppleScript adapter. No new
  dependencies, Swift changes, provider or model changes. Nebius Nemotron routing/
  writing and Qwen embeddings remain configured; no AI/provider call ran.

See `docs/production/design.md` P02 Task 2 contract for precise APIs and limits.

## Verification evidence

Interpreter for every test command:
`/Users/m1labs/Dev/apps/juno/.venv/bin/python -m pytest`.
All tests retain global synthetic Notes, temporary encrypted storage/policy,
provider credential fakes, and DNS/socket/subprocess-denial fixtures. No live
Apple data, provider, private cache, native helper or listener was accessed.

| Check | Observed result |
|---|---|
| Root-authored initial Task 2 race suite, rerun before implementation | 21 failed; organizer/planner failures demonstrate actual user-edit loss |
| Expanded race + Notes adapter tests before core changes | 35 failed, 7 passed |
| v1 migration regression before migration support | 1 failed, unsupported observed_revision argument |
| Core race/Notes/operation tests after guarded API + producers | 54 passed |
| Filer duplicate titles/source ambiguity/approved-creation job before fixes | 3 failed, 1 passed |
| Care/reflection synthetic model mutations, restored baseline producer code | 2 failed; both incorrectly reported written and overwrote edits |
| Stale append anchor + truthful organizer result before fixes | 3 failed |
| Corrected journal-layout fixture before stale-layout guard | 1 failed; filed after user changed journal layout |
| Source deleted/edited during classification before source guards | 2 failed; stale text copied into destination |
| Final-read policy change, backup-time rename, rich empty-result before fixes | 3 failed; two verified audit cases already passed |
| Required expanded focused suite: tests/test_write_races.py tests/test_executor.py tests/test_notes.py tests/test_rewrite.py tests/test_filer.py tests/test_nodes.py tests/test_operations.py -o addopts='' -q | 195 passed in 1.95s |
| Final full suite: tests -o addopts='' -q | **768 passed in 4.79s**, exit 0 |
| git diff --check | Exit 0 |

Baseline was 714 tests. Final suite adds 54 net cases. Task2 owns and commits the
original root acceptance file; no exclusions remain. Old executor fakes that
returned the prewrite body forever now store writes. The unsafe old one-read
replace test is replaced by final-read/post-read verification. Existing dispatcher
fakes target apply_write. An obsolete title-only cache assertion was updated to
require reclassification instead of silently selecting a new same-title note.
No policy/revision/postwrite check was weakened for compatibility.

## Limits, follow-up ownership and rollback

- Remote Notes/editor/iCloud writers do not honor the local lock. Apple has no CAS;
  edits inside the final read/write gap can still be overwritten without a visible
  post-write mismatch. A synthetic test demonstrates that exact residual limit.
- Native metadata AppleScript behavior, real HTML normalization, permission prompts,
  multi-device races and rich-content fidelity are not verified by these tests.
  Conservative rejection may refuse ordinary but unfamiliar Apple formatting.
- Task 3 owns asynchronous audit/receipt reconciliation, cross-effect filing repair,
  and action adapters. APPLIED is verified Notes body evidence, not a final receipt.
  Approved creation may finish needs_review after add_home purges the active request;
  same request replay is blocked, including purged-payload refusal.
- Task 4 owns durable undo peek/verify/consume and snapshot provenance. Undo still
  pops before restore; backup writes can overwrite an older slot before a later
  final check refuses. This task prevents stale restore proposals from overwriting
  later edits but does not claim the complete undo lifecycle is implemented.
- Task 6 owns worker-wide lifecycle/leases, maintenance-job admission and legacy
  migration. P06 signed startup pause remains; this does not authorize real use.
- Rollback preserves a matched ledger/payload backup. Schema-1 code rejects schema 2;
  use schema-2-compatible code or a deliberate matched backup restore, never delete
  identity/tombstone files or reset the ledger to force retry. No automatic downgrade.
- No merge, push, deployment, release, dependency install or real data migration.

Next: root independent spec/quality review of this task-owned commit.


## Independent-review corrections (after 1487fff)

The independent review identified two important integration gaps. Both were
reproduced against production paths before correction:

1. Approved creation omitted source guards that existing-note filing already used.
   Editing/removing a proposed source paragraph or grouped part before `yes` still
   created a note containing the old text. Creation now receives persisted source
   IDs/revisions/anchors for every lead/part and the approval line. Both executor
   boundaries validate under the same lock. Refusal keeps the proposal pending;
   legacy snapshots without revisions cannot be rebound at approval. A stale
   duplicate-source proposal now conservatively refuses instead of guessing.
2. Rich-note results existed only in State.answer, which the normal listener did
   not visibly deliver. Organizer now captures the existing registered plain Ask
   note before inference and proposes a guarded append there. Generated alternatives,
   empty/short-model refusals and unsupported confirmations are visible in Ask,
   with zero rich-source writes and no automatic note creation. Missing, denied,
   rich or changed Ask targets generate failed results; graph request status is
   NEEDS_REVIEW, never silently completed with no result.

Evidence: **13 added regression cases**. The first 11 cases failed against the
pre-fix behavior. A final-boundary test fixture initially edited a seed phrase;
corrected it to mutate the actual source paragraph, then reran with creation source
checks removed and observed the expected failure before restoring the fix. Two
additional baseline-producer tests failed for missing legacy revision rebinding and
invisible unsupported confirmation. New tests run the real graph/request ledger
and assert actual Ask body plus request status, not only returned state.answer.

Corrected focused command `tests/test_filer.py tests/test_write_races.py -o addopts='' -q`:
**132 passed in 1.89s**. Corrected full command `tests -o addopts='' -q`:
**781 passed in 4.78s**, exit 0, with the same root interpreter and synthetic guards.
Whitespace checks passed. No provider/native/live data, dependencies, merge or push.
These corrections are in a separate task-owned fix commit; independent re-review
is the next gate. All schema/iCloud/Task3/4/6 limitations above remain unchanged.
